<%- include("partials/header")-%>

  <h1 class="cust">Honest reviews</h1>
  <%for(var i=0;i<reviews.length;i++){%>
  <div class="container">
    <div class="rug">
      <div class="card">
        <div class=" card-body">
          <h5 class="card-title"><%=reviews[i].name%></h5>
          <h6><%=reviews[i].email%></h6>
          <p class="card-text">"<%=reviews[i].request%>"</p>
        </div>
      </div>

    </div>

</div>

<%}%>
